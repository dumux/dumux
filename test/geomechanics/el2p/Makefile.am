check_PROGRAMS = test_el2p

noinst_HEADERS := $(wildcard *.hh)
EXTRA_DIST:=$(wildcard grids/*.dgf) co2values.inc CMakeLists.txt

test_el2p_SOURCES = test_el2p.cc

include $(top_srcdir)/am/global-rules
